package net.nicolas.calcium.variables;

public class SlotConfig {
    public enum ItemMode {
        ALL,
        FIXED,
        TAG,
        RECIPE
    }

    public enum StackMode {
        SINGLE,
        STACK
    }
}